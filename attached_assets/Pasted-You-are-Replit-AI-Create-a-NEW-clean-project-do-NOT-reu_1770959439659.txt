You are Replit AI. Create a NEW clean project (do NOT reuse the old codebase) for a B2B multi-tenant SaaS called “P Profile for Business”.

NON-NEGOTIABLE:
- No OTP, no SMS, no auth complexity. Use simple demo login (email + password) with predefined demo accounts stored locally.
- No external API keys. No OCR. This is B2B only.
- Arabic-first UI, RTL-ready, enterprise clean design.
- Use Vite + React + TypeScript + Tailwind (or the existing UI kit if standard in your templates).
- Persist all data in localStorage (single source of truth store).
- Multi-tenant isolation: every record has companyId, and UI must never mix data between companies.

ROUTES (must exist):
- /login (demo login only)
- /dashboard (default after login)
- /assets
- /branches
- /users
- /reports
- /settings (subscription + company settings, NOT “coming soon”)

BOTTOM NAV / SIDEBAR (enterprise):
Dashboard, Assets, Branches, Reports, Users, Settings
Dashboard is default landing.

DATA MODEL (single source of truth):
Company:
{ id, name, plan: "BASIC"|"PRO"|"ENTERPRISE", assetLimit, createdAt }
Branch:
{ id, companyId, name, location, createdAt, updatedAt }
User:
{ id, companyId, name, email, role: "ADMIN"|"FINANCE"|"IT"|"MAINTENANCE", branchScope: "ALL"|string[], createdAt }
Asset:
{
  id, companyId, internalAssetNumber: "AST-0001",
  name, serialNumber,
  purchaseDate, warrantyMonths,
  warrantyEndDate?: string,
  branchId,
  responsibleUserId,
  status: "ACTIVE"|"MAINTENANCE"|"RETIRED",
  createdAt, updatedAt
}
MaintenanceTicket:
{ id, companyId, assetId, issue, status:"OPEN"|"IN_PROGRESS"|"CLOSED", openedByUserId, assignedToUserId?, openedAt, closedAt? }

WARRANTY LOGIC (centralized utility):
- warrantyEnd = manual warrantyEndDate OR purchaseDate + warrantyMonths
- status:
  ACTIVE (>30 days)
  EXPIRING_SOON (0–30 days)
  EXPIRED (<0 days)

DASHBOARD REQUIREMENTS:
- KPI cards: Total, Expiring Soon, Expired, Under Maintenance
- Table of expiring soon sorted by days remaining
- Filters: branch + warranty status
- Simple charts: assets per branch + warranty distribution (can be lightweight)
- Export Warranty CSV

ASSET REGISTRY:
- Enterprise table: search, filters (branch/status/warranty), sort, pagination (client-side ok)
- CRUD modal: create/edit/delete with confirm
- Auto-generate internal asset numbers per company
- Assign branch + responsible user
- Status updates

BRANCHES:
- CRUD + asset count
- Block delete if assets exist OR provide “Reassign assets then delete”

USERS & RBAC (UI enforced):
- Admin full
- Finance view + export only
- IT manage assets/branches
- Maintenance update asset status + tickets only
Enforce by disabling actions and blocking saves with clear Arabic messages.

REPORTS:
- Export CSV: Assets, Warranty, Maintenance (MAINTENANCE assets or tickets)
- Exports must respect active company + filters

SEED DEMO DATA BUTTON:
Add “Load Demo Data” in Settings that creates:
- 2 companies
- 3 branches each
- 6 users with different roles
- 25 assets with mixed warranty states + some in maintenance
Persist to localStorage.

QUALITY:
- No “coming soon” placeholders.
- Clean empty states in Arabic.
- Add data-testid for KPIs, filters, CRUD buttons, export buttons, table rows.
- No console errors.

DELIVER:
A working clean B2B SaaS demo with the above routes, navigation, localStorage persistence, and enterprise UI.