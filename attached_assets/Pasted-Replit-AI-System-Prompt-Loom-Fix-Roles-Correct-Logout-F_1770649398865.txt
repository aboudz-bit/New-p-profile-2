Replit AI System Prompt — Loom (Fix Roles + Correct Logout Flow + Separate Merchant/Staff from Customer)

GOAL:
Fix the role system and navigation so the app is clean and correct:
- Customer must NEVER see Merchant/Staff dashboard or “Staff Management” in Customer Profile.
- On LOGOUT (or first launch), show a simple role chooser: (1) Customer (2) Merchant/Store.
- Staff is NOT a public option on logout screen. Staff logs in through “Merchant” flow and is routed automatically based on role after OTP.
- Implement proper role-based routing + UI guards everywhere.
- Keep the app as ONE project (same Loom app), but with role-based experiences.

CRITICAL FIXES REQUIRED:
1) Remove Staff Management from the CUSTOMER profile completely.
2) Add a Role Selection screen that appears:
   - On app start if no role selected yet OR after Logout
   - Two buttons only: “Customer” and “Merchant / Store”
3) Persist selected role in localStorage (e.g., loom_selected_role = "customer" | "merchant").
4) OTP login:
   - Customer flow: OTP → customer session → customer routes
   - Merchant flow: OTP → check user role from DB (merchant_owner or merchant_staff)
       - If merchant_owner → merchant dashboard
       - If merchant_staff → staff dashboard with permissions
   - If user selected “Merchant” but account is actually customer-only → block and show “This phone is not registered as a merchant/staff. Apply to become a store.”
   - If user selected “Customer” but phone is merchant/staff → allow login as customer ONLY if we explicitly allow dual-role; otherwise show “Use Merchant login”. (Choose: allow dual-role = YES for now, but keep separate entry points.)

IMPLEMENTATION DETAILS (DO THIS EXACTLY):

A) DATA MODEL (server/db)
- users table must include:
  - id
  - phone
  - role ENUM: "customer" | "merchant_owner" | "merchant_staff" | "admin"
  - merchant_id nullable
  - is_active boolean
- staff_permissions table:
  - id
  - staff_user_id
  - merchant_id
  - branches (string[] or json)
  - permissions (json) e.g.:
    {
      "products": { "view": true, "create": true, "edit": true, "delete": false, "approve": false },
      "orders": { "view": true, "update_status": true, "refunds": false },
      "finance": { "view": false, "payouts": false },
      "analytics": { "view": false },
      "shipping": { "view": true, "update": true },
      "staff": { "manage": false }
    }

B) AUTH + ROLE STATE (client)
- Create a single auth store (zustand or existing store) with:
  - sessionUser: { id, phone, role, merchantId }
  - selectedEntryRole: "customer" | "merchant" (from localStorage)
  - setSelectedEntryRole(role)
  - loginWithOtp(phone, otp, entryRole)
  - logout() -> clear session + clear sensitive stores + navigate to /role

- On logout:
  - clear user session
  - clear cart, favorites, filters, and any merchant dashboards state
  - DO NOT keep old role routes accessible

C) NEW SCREEN: RoleChooser
- Route: /role
- UI: clean card with Loom logo + two buttons:
  1) “Customer”
  2) “Merchant / Store”
- When user taps:
  - store localStorage loom_selected_role
  - navigate to /login (single login screen) passing entry role

D) LOGIN SCREEN BEHAVIOR
- Route: /login
- Read entry role from store/localStorage
- Title changes:
  - Customer: “Sign in to Loom”
  - Merchant: “Store / Staff Sign in”
- OTP request + verify as you already do, but after verify call backend /auth/verifyOtp which returns user role and merchantId.
- After verify:
  - if entryRole == "customer":
      - route by actual role:
        - customer -> /home
        - merchant_owner OR merchant_staff -> allow as customer ONLY if we set allowDualRole = true:
            - set session as customer_view_mode and hide merchant tabs (customer UI only)
            - OR show modal “Continue as Customer or Merchant?” (but user asked for only two options on logout; so default to customer here)
  - if entryRole == "merchant":
      - if role == merchant_owner -> /merchant/dashboard
      - if role == merchant_staff -> /merchant/staff/dashboard
      - else -> show blocked screen “Not registered as store/staff” + CTA “Apply as Store”

E) ROUTE GUARDS (MUST)
Implement a protected route wrapper:
- CustomerRoutes: only allow sessionUser.role in ["customer"] OR (customer_view_mode true)
- MerchantRoutes: only allow ["merchant_owner","merchant_staff"]
- AdminRoutes: only allow ["admin"]

If user tries to access merchant pages while customer:
- redirect to /role or /login with correct message

F) UI NAVIGATION (Bottom Bar)
- Customer bottom bar:
  Home | Stores | Market | Cart | Profile
- Merchant Owner bottom bar:
  Dashboard | Orders | Products | Finance | Profile
- Merchant Staff bottom bar:
  Dashboard | Orders | Products | Profile | (optional: Shipping) based on permission
- Admin bottom bar (optional):
  Admin | Merchants | Orders | Ads | Profile

IMPORTANT:
- Customer Profile must NOT show “Staff Management”.
- Merchant Profile can show:
  Staff Management (owners only)
  Branches
  Payout/IBAN
  Tax/CR info
- Staff Profile must NOT show “Staff Management” (only owner can manage staff).

G) FIX CURRENT BUG: “Staff Management appears in customer profile”
- Locate the component that renders the Profile menu cards.
- Wrap Staff Management card with:
  if (sessionUser.role === "merchant_owner") show it; else hide.
- Ensure it is not shown for customer or merchant_staff.

H) SEED / DEMO DATA
- Keep demo data; ensure:
  - Some phones are customers
  - Some phones are merchant owners
  - Some phones are merchant staff
- Provide at least:
  - 2 merchant owners
  - each with 2 staff accounts
  - staff accounts have different permissions
- Add a simple “DEV TOOLS” section (dev-only) to quickly switch a phone’s role for testing.

I) TEST CHECKLIST (MUST PASS)
1) Logout -> always goes to /role and shows ONLY two options (Customer, Merchant/Store).
2) Customer login -> never sees Staff Management or Merchant dashboard.
3) Merchant login:
   - Owner -> sees Dashboard with Staff Management accessible
   - Staff -> sees Dashboard but cannot access Staff Management; permissions restrict pages
4) Direct URL access to /merchant/* as customer -> redirects away.
5) After logout, back button should not reopen protected pages (use replace navigation + guards).

DELIVERABLE:
Implement all changes in the existing Loom project, using the current stack and routing (wouter). Update the relevant files:
- client/src/App.tsx (routes + guards)
- client/src/pages/role.tsx (new)
- client/src/pages/login.tsx (entry role aware)
- client/src/pages/profile.tsx (hide staff management for non-owner)
- merchant pages under client/src/pages/merchant/*
- auth store + helpers
- server auth endpoints + db schema updates as needed

DO NOT:
- Create a separate app/repo
- Add extra role options on logout screen
- Leave any merchant/staff UI inside customer profile

After implementing, run through the checklist and ensure it works on mobile preview.