You are working on our B2B SaaS app (Asset Registry + Warranty tracking). Implement the items below as ONE integrated upgrade across the whole app.

⚠️ NON-NEGOTIABLE:
- DO NOT implement OTP.
- DO NOT add SMS verification.
- DO NOT modify the current authentication flow.
- Assume user is already authenticated.
- Keep everything session-based and simple.
- Do NOT expose any API keys in client code or UI.

Goal: Make the app feel enterprise/production-grade, but still safe if backend is not finalized (frontend-first with mock/store is acceptable).

====================================================
A) B2B CORE MODULES (MUST EXIST)
====================================================

1) Asset Registry (سجل الأصول)
Each asset must support:
- Internal Asset Number (auto like AST-0001, AST-0002…)
- Serial Number
- Purchase Date
- Warranty (Months)
- Warranty End Date (calculated or manual override)
- Location (Branch)
- Responsible (employee/user)
- Status: ACTIVE | MAINTENANCE | RETIRED
- Company isolation (companyId) so data never mixes across companies.

2) Warranty Tracking Dashboard (لوحة الضمانات)
A real management dashboard:
- Upcoming warranty expirations
- Expired warranties
- Under maintenance assets
- Branch-level breakdown
- Exportable reports

3) Multi-Branch Support
Companies can have multiple branches:
- Warehouse
- Office
- Branch A
- Branch B
Every asset belongs to one branch.

4) Role-Based Access (RBAC)
Roles (at minimum):
- Admin
- Finance
- IT
- Maintenance
UI must be ready for RBAC (even if enforcement is mock).
Example permissions:
- Admin: full access
- Finance: view/export reports + view assets
- IT: manage assets + branches
- Maintenance: view assets + update status/maintenance

5) Export / Reporting
Must support exporting:
- Assets report
- Warranty report
- Maintenance/Failures report (basic version OK)

====================================================
B) UI / NAV STRUCTURE (ENTERPRISE FEEL)
====================================================

Routes:
- /dashboard  (NEW) Warranty Tracking Dashboard (default after login)
- /assets     Asset Registry (existing tab)
- /branches   Branch management screen
- /users      Users & Roles screen (RBAC UI)
- /reports    Reports & exports screen

Bottom navigation:
- Add “Dashboard”
- Keep “Assets”
- Add “Branches”
- Add “Reports”
- Add “Users”
Make Dashboard the default landing page after login.

UI Requirements:
- Enterprise SaaS feel (clean spacing, subtle shadows, strong hierarchy)
- RTL ready (Arabic UI capable; use dir="auto" where needed)
- Responsive (mobile/tablet/desktop)
- Avoid English-only hardcoding (prefer bilingual labels or Arabic-first)
- Add data-testid for key elements (filters, actions, rows, KPI cards)

====================================================
C) ASSET REGISTRY MODULE (KEEP + ENHANCE)
====================================================

Asset table view:
- Search
- Filters:
  - Branch
  - Asset Status (ACTIVE/MAINTENANCE/RETIRED)
  - Warranty Status (ACTIVE/EXPIRING_SOON/EXPIRED)
- Sort (name, purchase date, warranty end, etc.)

CRUD UI:
- Create asset (auto internal number)
- Edit asset (row click)
- Delete asset (confirm)
- Assign branch + responsible user
- Update status
- Warranty fields editable with safe validation

Company isolation:
- Company selector (mock) that filters all assets by companyId

====================================================
D) WARRANTY LOGIC (CRITICAL)
====================================================

Warranty Status rules:
- ACTIVE → warrantyEndDate > 30 days from today
- EXPIRING_SOON → warrantyEndDate within 0–30 days
- EXPIRED → warrantyEndDate < today

Warranty end date:
- If warrantyEndDate exists manually, use it.
- Else calculate:
  warrantyEndDate = purchaseDate + warrantyMonths

All computations must be dynamic and consistent across Dashboard + Assets + Reports.

====================================================
E) NEW MODULE: /dashboard (Warranty Tracking Dashboard)
====================================================

1) KPI Cards (top section)
Create 4 KPI cards (responsive grid):
1) Total Assets
2) Expiring Soon (≤ 30 days)
3) Expired
4) Under Maintenance (status === MAINTENANCE)

Requirements:
- Color-coded badges
- Clickable (click filters the table below)
- data-testid for each KPI

2) Expiring/Status Table (below KPIs)
Columns:
- Internal Asset #
- Asset Name
- Serial Number
- Branch
- Responsible
- Warranty End Date
- Days Remaining
- Warranty Status badge

Behavior:
- Default sort by Days Remaining ASC
- Filter by Branch
- Filter by Warranty Status
- Company isolation respected
- data-testid on filters + rows

3) Charts (enterprise feel)
Add:
- Bar chart: Assets per Branch
- Pie chart: Warranty status distribution

If no chart library is available, create simple styled “mock charts” using div bars/circles (no heavy dependency required).

4) Export Button
Button: “Export Warranty Report”
- CSV export
- Fields:
  Internal Asset #
  Name
  Serial
  Branch
  Purchase Date
  Warranty Months
  Warranty End Date
  Warranty Status
  Responsible
  Asset Status

Frontend-only export is acceptable.

====================================================
F) /branches (Multi-Branch Management)
====================================================

Branch list view:
- Branch name
- City/Location (simple string OK)
- Branch code (optional)
- Asset count (derived)

CRUD:
- Create branch
- Edit branch
- Delete branch (block delete if assets exist OR ask to reassign)

Company isolation respected.

====================================================
G) /users (Users & Roles UI)
====================================================

UI to manage (mock is fine):
- List users (name, email/phone optional, role, branch scope)
- Assign role per user
- Branch scope: user can be tied to “All branches” or specific branch

No OTP, no SMS, no new auth flow.

====================================================
H) /reports (Export + Reporting)
====================================================

Reports page with 3 export panels:
1) Assets Report (CSV)
2) Warranty Report (CSV)
3) Maintenance/Failures Report (CSV)
   - Use status/history if exists; otherwise export assets currently in MAINTENANCE plus lastUpdated date.

Add date range filters where possible (optional if mock).

====================================================
I) DATA MODEL (SINGLE SOURCE OF TRUTH)
====================================================

Asset object must include:
{
  id: string,
  companyId: string,
  internalAssetNumber: string, // AST-0001
  name: string,
  serialNumber: string,
  purchaseDate: string (ISO),
  warrantyMonths: number,
  warrantyEndDate?: string (ISO),
  branchId: string,
  responsibleUser: { id: string, name: string },
  status: "ACTIVE" | "MAINTENANCE" | "RETIRED",
  createdAt: string (ISO),
  updatedAt: string (ISO)
}

Branch object:
{
  id: string,
  companyId: string,
  name: string,
  location?: string,
  createdAt: string,
  updatedAt: string
}

User object:
{
  id: string,
  companyId: string,
  name: string,
  role: "ADMIN" | "FINANCE" | "IT" | "MAINTENANCE",
  branchScope: "ALL" | string[] // branchIds
}

If backend is not available, implement an in-memory store (client-side) that persists using localStorage (preferred) so refresh doesn’t wipe everything.

====================================================
J) DO NOT BREAK EXISTING WORK
====================================================

- Do NOT break Asset Registry
- Keep all existing screens working
- Ensure routes + nav are consistent
- No crashes, clean console
- Add sensible empty states

====================================================
K) DELIVERABLE
====================================================

Implement all modules above with production-feeling UI, RTL-ready styling, and consistent warranty logic.
No OTP anywhere.