FIX “LIKES / WISHLIST” PAGE IN LOOM (CURRENT PROJECT) — MAKE IT FULLY FUNCTIONAL

Problem:
In /cart the “likes” UI is not practical. It shows a grid with checkboxes/hearts but no clear Wishlist page, no actions, and it’s confusing.

Goal:
Create a proper Wishlist experience + make Cart page clean and usable.

REQUIREMENTS (DO ALL):

1) CREATE DEDICATED WISHLIST PAGE
- Route: /wishlist
- Bottom nav MUST have a Wishlist/❤️ tab (or replace the current heart icon in header with a nav tab)
- Page title: “Wishlist” / “المفضلة”
- Show liked items as a clean grid (2 columns on mobile)
- Each card shows:
  - image
  - name
  - price
  - store name
  - quick actions:
    - ❤️ remove from wishlist (top-right overlay)
    - 🛒 add to cart (bottom-right overlay)
- Tapping the card (not the icons) opens the product viewer.

2) CART PAGE SHOULD BE CART ONLY
- Route: /cart becomes CART only (no mixed wishlist grid)
- Cart page must show:
  - list of cart items with qty +/- and remove
  - subtotal
  - checkout button
- Add a small secondary button/link at top: “Go to Wishlist” / “اذهب للمفضلة”

3) OPTIONAL COMPARE MODE (ONLY IF EASY)
- In /wishlist add “Compare” toggle:
  - user can select up to 2 items (checkbox visible only in compare mode)
  - show compare sheet (side-by-side name/price/store + “Add to cart” for each)
- If time is short, skip compare mode, but MUST finish (1) and (2).

4) STATE + STORAGE (CRITICAL)
- Use the SAME global favorites store (localStorage) already used in Explore viewer and market grid.
- Ensure favorites persist after refresh.
- Wishlist page must read from that store and update instantly.

5) BUTTONS MUST WORK ON iOS
- Fix any overlay click issues:
  - icons are <button> with aria-label
  - use onPointerDown + onClick:
    e.preventDefault(); e.stopPropagation();
- Ensure z-index + pointer-events allow tapping icons.
- Make tap targets >= 44x44.

6) NAVIGATION
- Ensure bottom nav routes:
  - Home
  - Stores
  - Market
  - Cart
  - Profile
  - Wishlist (add as a 6th tab OR replace a non-essential one—choose best UX)
- No dead buttons anywhere.

7) REMOVE “UNPRACTICAL” UI
- Remove the current wishlist-like grid/check UI from /cart that looks like selection mode.
- Wishlist should be clean, not confusing.

DELIVERABLE:
- Implement code changes in the existing app.
- Verify: liking an item in Explore/grid → appears in /wishlist.
- From /wishlist: “Add to cart” works and shows toast.
- /cart shows actual cart items, not wishlist grid.

DO IT NOW IN THE CURRENT LOOM CODEBASE.